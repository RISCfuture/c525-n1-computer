# frozen_string_literal: true

source "https://rubygems.org"

# fastlane is used for one thing here: syncing the Developer ID certificate out
# of RISCfuture/certificates so the macOS .xpl can be signed and notarized.
gem "fastlane", "~> 2.228"
